# 🌸 花园种植游戏 — 待办事项清单

> 基于策划文档 vs 代码实现的对比审计，最后更新：2026-02-28

---

## 一、Bug 修复（高优先级）

### 1.1 配置数值与策划不一致
- [ ] `INITIAL_COINS` 代码为 1000，策划文档为 0
- [ ] `INITIAL_WATER` 代码为 101，策划文档为 100（且超过 MAX_WATER=100）
- **处理方式**：确认是测试遗留还是策划变更，统一代码与文档

### 1.2 harvestPot 闭包过期风险
- [ ] `usePots.ts` 中 `harvestPot` 从闭包读取 `pots`（而非 `setPots(prev => ...)` 的 prev），快速连续收割可能读到旧状态
- **修复方式**：将守卫判断和等级查询移入 `setPots` 的 updater 函数内

### 1.3 花朵升级扣除顺序问题
- [ ] `useFlowerLevels.ts` 先扣魂再扣金币，若 `spendCoins` 失败魂不会退还
- **修复方式**：先扣金币再扣魂，或引入事务回滚

---

## 二、死代码清理

### 2.1 `growing` 状态从未被使用
- [ ] `PotState` 类型中定义了 `growing`，但 `waterPot()` 直接从 `seeded → blooming`，跳过了 `growing`
- [ ] 每种花的 `sprout` 图片素材已就绪但从未展示
- **选择A**：激活 growing 状态（加入浇水后的生长计时器，显示嫩芽阶段）
- **选择B**：删除 growing 状态和 sprout 图片引用

### 2.2 `harvested` 状态从未被使用
- [ ] `PotState` 中定义但无任何代码路径进入，可删除

---

## 三、缺失素材

### 3.1 种子图标缺少 6 种花
`public/art/ui/` 中仅有 4 种花的种子图标：

| 花朵 | 图标状态 |
|------|---------|
| 玫瑰 rose | ✅ 已有 |
| 郁金香 tulip | ✅ 已有 |
| 雏菊 daisy | ✅ 已有 |
| 向日葵 sunflower | ✅ 已有 |
| 薰衣草 lavender | ❌ **缺失** |
| 兰花 orchid | ❌ **缺失** |
| 牡丹 peony | ❌ **缺失** |
| 康乃馨 carnation | ❌ **缺失** |
| 菊花 chrysanthemum | ❌ **缺失** |
| 扶桑 hibiscus | ❌ **缺失** |

- **需要制作**：`icon_seed_{id}.svg` × 6 放入 `build/public/art/ui/`

---

## 四、代码健壮性改进

### 4.1 拖拽阈值硬编码
- [ ] `GameScene.tsx` 中拖拽阈值硬编码为 `8`，应改为导入 `DRAG_THRESHOLD_PX`

### 4.2 添加 React Error Boundary
- [ ] 当前无错误边界包裹 `GameScene`，损坏的 localStorage 数据可能导致白屏崩溃
- [ ] 添加 ErrorBoundary 组件，捕获异常时提示用户并提供"清除存档重新开始"的恢复方案

### 4.3 存档数据校验
- [ ] `loadSave()` 仅检查 `version === 1`，未校验字段完整性
- [ ] 版本升级时应添加迁移逻辑（如新增花朵种类后旧存档缺少对应字段）

---

## 五、玩法体验优化

### 5.1 激活 growing（嫩芽）阶段
- [ ] 浇水后不立即开花，先进入 `growing` 阶段（展示已有的 sprout 素材），经过短暂生长时间后再进入 `blooming`
- [ ] 增加浇水→生长→开花的期待感

### 5.2 离线进度补偿
- [ ] 重新打开页面时计算离线时间，补偿水量恢复和花盆冷却进度
- [ ] 当前关闭页面后所有定时器进度丢失

### 5.3 收购订单到达提醒
- [ ] 新订单生成时添加视觉提示（如工具栏按钮闪烁/弹出通知）
- [ ] 订单即将过期时添加倒计时警告

### 5.4 花盆冷却进度可视化
- [ ] 花盆在 `cooling` 状态显示环形或条形倒计时进度条

### 5.5 采购任务冷却进度条
- [ ] `getTaskCooldownRemaining` 已暴露但面板可能只显示文字
- [ ] 添加进度条动画让等待体验更好

---

## 六、内容扩展

### 6.1 更多花盆皮肤
- [ ] 当前仅 2 款皮肤（经典赤陶 + 青花瓷），建议扩展 3~5 款
- [ ] 建议价格梯度：500 / 1000 / 2000 / 5000

### 6.2 中后期目标缺失
- [ ] 玩家 Lv.5 后不再解锁新花朵，Lv.6~10 无内容激励
- [ ] **建议方案**：
  - 成就系统（首次种满 28 盆、收割 100 次、集齐所有花等）
  - 高级皮肤在高玩家等级解锁
  - 花园装饰物/背景主题切换

### 6.3 花朵品种扩展
- [ ] 目前 10 种花朵，配置编辑器已支持新增
- [ ] 建议后续批次：百合 lily、樱花 sakura、紫罗兰 violet 等

---

## 七、UI / 体验打磨

### 7.1 音效系统
- [ ] 无任何音频，建议添加：
  - 播种：轻柔落地声
  - 浇水：水滴声
  - 收割：清脆采摘声
  - 升级：叮咚提示音
  - 背景音乐（可选，带开关）

### 7.2 新手引导
- [ ] 首次进入游戏无任何提示，新玩家可能不知道该做什么
- [ ] 建议添加简单的 3~5 步引导遮罩

### 7.3 花棚背景丰富化
- [ ] 当前仅单层 `bg_greenhouse.svg`
- [ ] 可考虑：昼夜变化、天气效果、装饰物

### 7.4 长屏幕滚动优化
- [ ] 4×7 花盆高度约 670px，短屏手机底部花盆可能被裁切
- [ ] 改为动态高度 + overflow-y 滚动

---

## 八、工程 / 效率

### 8.1 Git 规范
- [ ] `node_modules` 曾被意外跟踪（已修复添加 .gitignore）
- [x] 已推送到 GitHub（`dmxzxy/flowers`）

### 8.2 构建产物
- [ ] 未进行过 `npm run build` 生产构建测试
- [ ] 建议验证构建产物可正常运行

### 8.3 配置编辑器
- [x] 独立 HTML 工具已创建
- [x] 已集成 Vite 插件（`/__editor` 访问，保存即生效 HMR）
- [ ] 编辑器目前不支持导入已有项目配置（仅手动写在 state 中）

---

## 进度概览

| 类别 | 总计 | 完成 | 状态 |
|------|------|------|------|
| 核心玩法 | 12 项 | 12 项 | ✅ 100% |
| 经济系统 | 8 项 | 8 项 | ✅ 100% |
| UI 面板 | 9 项 | 9 项 | ✅ 100% |
| 存档系统 | 6 项 | 6 项 | ✅ 100% |
| 特效动画 | 8 项 | 8 项 | ✅ 100% |
| 批量操作 | 3 项 | 3 项 | ✅ 100% |
| Bug 修复 | 3 项 | 0 项 | 🔴 待修 |
| 死代码清理 | 2 项 | 0 项 | 🟡 待定 |
| 缺失素材 | 6 项 | 0 项 | 🔴 待补 |
| 健壮性改进 | 3 项 | 0 项 | 🟡 建议 |
| 体验优化 | 5 项 | 0 项 | 🟡 建议 |
| 内容扩展 | 3 项 | 0 项 | ⚪ 未来 |
| UI 打磨 | 4 项 | 0 项 | ⚪ 未来 |

**核心功能完成度：100%** — 策划文档中的所有系统均已实现并可运行。
**推荐下一步**：先修 Bug（§一）→ 补素材（§三）→ 激活 growing 阶段（§五.1）→ 离线补偿（§五.2）。
